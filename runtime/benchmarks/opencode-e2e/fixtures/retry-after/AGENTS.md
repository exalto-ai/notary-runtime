# Retry-After fixture

This is a synthetic, dependency-free Python repository used only by the LLM
Notary OpenCode canary.

- Implement only the task in `TASK.md`.
- Modify only `retry_after.py`.
- Use only the Python standard library.
- The only allowed shell command is `python3 -m unittest -v`.
- Do not use Git, the network, subagents, skills, external files, or environment
  inspection.
